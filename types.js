/* @flow */
'use strict'

/* ::
export type BmRequest = {
    body : any,
    url: {
        host: string,
        hostname: string,
        pathname: string,
        protocol: 'http:' | 'https:',
        query: BlobQuery
    }
}

export type BlobQuery = {
    id?: string
}

export type BmResponse = {
    getUrl: string,
    postUrl: string,
    id: string
}

*/
